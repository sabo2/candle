{
  "name": "pzpr-canvas",
  "version": "0.7.0",
  "description": "This is a support script for pzpr.js",
  "main": "./dist/candle.js",
  "keywords": [
    "graphic",
    "HTML5",
    "canvas",
    "SVG"
  ],
  "author": "sabo2",
  "license": "MIT",
  "scripts": {
    "build": "grunt default",
    "release": "grunt release",
    "lint": "grunt jshint",
    "test": "grunt jshint && mocha -R progress --recursive test",
    "prepublish": "in-publish && npm run release || not-in-publish"
  },
  "devDependencies": {
    "grunt": "^1.0.1",
    "grunt-contrib-clean": "^1.0.0",
    "grunt-contrib-concat": "^1.0.1",
    "grunt-contrib-copy": "^1.0.0",
    "grunt-contrib-jshint": "^1.1.0",
    "grunt-contrib-uglify": "^2.2.0",
    "grunt-newer": "^1.1.1",
    "in-publish": "^2.0.0"
  }
}
