{
  "name": "pzpr-canvas",
  "version": "0.8.2",
  "description": "This is a support script for pzpr.js",
  "engines": {
    "npm": ">=5.6.0"
  },
  "main": "./dist/candle.js",
  "keywords": [
    "graphic",
    "HTML5",
    "canvas",
    "SVG"
  ],
  "author": "sabo2",
  "license": "MIT",
  "scripts": {
    "build": "grunt default",
    "release": "grunt release",
    "lint": "grunt jshint",
    "test": "grunt jshint && mocha --recursive test",
    "clean": "grunt clean",
    "prepublish": "npm run release && npm test"
  },
  "devDependencies": {
    "grunt": "^1.0.1",
    "grunt-contrib-clean": "^1.0.0",
    "grunt-contrib-concat": "^1.0.1",
    "grunt-contrib-copy": "^1.0.0",
    "grunt-contrib-jshint": "^1.1.0",
    "grunt-contrib-uglify": "^2.2.0",
    "grunt-newer": "^1.1.1",
    "mocha": "^6.1.4"
  }
}
